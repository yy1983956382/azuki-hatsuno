# 红豆泥网站项目日志

最后整理：2026-09-07
线上地址：https://yy1983956382.github.io/azuki-hatsuno/

## 一、项目现状

这是一个不依赖后端服务器的静态网站，使用 HTML、CSS 和原生 JavaScript 构建，并通过 GitHub Pages 发布。

当前栏目：

1. **天气**：杭州与秦野的时间、阳历、农历、天气提示和模拟时钟。
2. **揭示板**：读取 `note.txt`，显示可随文件更新的留言。
3. **转盘选择器**：最多八个选项，可编辑、删除、重置和随机抽选；选项保存在访问者自己的浏览器中。

首页附加内容：

- 城市背景图每次打开或刷新时随机轮换。
- 单词角从 `wordlibrary.json` 的 5,391 条记录中随机显示单词、音标、中日释义。
- 桌面端城市卡片左右排列，移动端改为上下排列的 4:3 横版卡片。

## 二、历史日程

| 日期 | 主要内容 |
| --- | --- |
| 2026-05-25 | 建立本地网站雏形；完成品牌、双城天气时间卡片、阳历与农历、模拟时钟、随机景观图和揭示板；开始使用 GitHub Pages 发布。 |
| 2026-05-25 至 05-26 | 多轮调整字体、横向排版、日期写法、时钟样式、移动端布局和天气提示；图片库改为杭州与秦野两个目录。 |
| 2026-05-26 | 增加 GitHub 图片目录读取，使新上传图片可自动进入随机轮换；为天气增加多来源回退。 |
| 2026-05-27 | 加入 `wordlibrary.xlsx`，生成浏览器使用的 `wordlibrary.json`；完成随机单词角和独立音标字体。 |
| 2026-05 至 06 | 曾制作资讯栏目，后因速度、来源筛选和稳定性问题完整移除；`news.txt` 不再使用。 |
| 2026-05-31 至 06-07 | 新增全页面“转盘选择器”；完成八项上限、淡色扇区、放射文字、直接编辑、本地保存和重置按钮。 |
| 2026-07-31 | 进行项目全量检查；核对线上文件、接口、桌面与移动布局、转盘交互；清理旧天气代码和过期资料，补齐图片备用清单并加入项目日志。 |
| 2026-09-07 | 将桌面项目正式连接到 GitHub 仓库 `yy1983956382/azuki-hatsuno`；同步本地项目资料，建立本地提交和直接推送流程。 |

已经讨论但暂不制作的栏目：桌面 ChatGPT 直连、富士山云层预报、斯普拉顿 3 鲑鱼跑日程。

## 三、文件与数据关系

| 文件或目录 | 用途 | 更新注意事项 |
| --- | --- | --- |
| `index.html` | 页面结构与栏目入口 | 修改后与 CSS、JS 一起检查版本参数。 |
| `styles.css` | 桌面端和移动端样式 | 当前为单文件，后续大改建议按功能拆分。 |
| `app.js` | 时间、天气、图片、单词、揭示板和转盘逻辑 | 当前功能集中，后续大改建议拆成模块。 |
| `note.txt` | 揭示板内容 | 上传到 GitHub 后，Pages 完成部署即可同步。 |
| `photolibrary/` | 两座城市的景观图片 | 新图应放入对应城市目录；文件名建议连续、只用英文和数字。 |
| `wordlibrary.xlsx` | 人工维护的原始单词库 | 浏览器不会直接读取它。 |
| `wordlibrary.json` | 网站实际读取的单词库 | 修改 Excel 后必须重新生成 JSON，当前尚无自动转换脚本。 |
| `.nojekyll` | 告知 GitHub Pages 按静态文件发布 | 保留即可。 |
| `start-local-site.*` | 本机预览服务器 | 双击 BAT 后访问 `http://127.0.0.1:8787/`。 |

## 四、2026-07-31 检查结果

已通过：

- 线上核心文件与本地文件一致。
- GitHub Pages、GitHub 图片目录、Open-Meteo、wttr.in、日本气象厅均能正常响应。
- `app.js` 语法检查通过；单词 JSON 共 5,391 条，四个字段均无缺失。
- 16 张城市图均为约 3:4 的 1000 像素宽图片。
- 1440 像素桌面、390 像素手机和手机转盘页均无横向溢出。
- 转盘的空白添加、编辑、随机抽选、保存和重置流程可用。

本次已整理：

- 杭州备用图片清单补入 `hangzhou07.jpg`。
- 删除未启用的旧中国气象局天气代码分支。
- 修正旧资料中已经移除的资讯栏目说明。
- 添加站点图标，消除浏览器的图标 404 请求。
- 转盘中心改为可通过键盘操作的按钮。
- 添加 `.gitignore`，避免常见临时文件混入项目。

## 五、后续大改前的重点

1. **保持版本保护**：本地目录已建立 Git，并跟踪 `origin/main`。后续每次修改都应先验证，再提交和推送；大改使用独立分支。
2. **拆分代码**：`app.js` 约 900 行、`styles.css` 约 950 行。建议按首页基础、天气、相册、单词、揭示板、转盘分别拆分。
3. **减少无效请求**：揭示板目前每 3 秒请求一次；单词库每次强制下载约 621 KB；进入转盘页时隐藏的天气和相册仍会加载。
4. **改进图片加载**：图片总计约 32.6 MB，单张最高约 2.9 MB。建议保留原图备份，网站使用压缩后的 WebP 或 AVIF。
5. **降低外部依赖风险**：图片目录依赖未登录的 GitHub API，每个 IP 通常有请求额度；后续可改为自动生成图片清单。
6. **补齐数据工具**：增加“Excel 转 JSON”和“扫描图片目录生成清单”的脚本，避免每次手工同步。
7. **建立发布边界**：将真正上线的文件放入独立 `public/` 目录，避免开发资料被误上传。
8. **增加自动检查**：至少保留 JavaScript 语法检查、桌面/手机截图和核心交互测试。

## 六、建议改造日程

| 阶段 | 建议耗时 | 目标 |
| --- | --- | --- |
| 0. 版本保护（已完成） | 半天 | 已建立本地 Git、连接远端仓库并验证直接推送。大改开始时再创建独立分支。 |
| 1. 目录与模块整理 | 1 至 2 天 | 建立 `public/`、`src/`、`tools/`，拆分 JS/CSS，保持现有页面功能不变。 |
| 2. 数据和图片流水线 | 1 天 | 自动生成单词 JSON、图片清单，压缩网页图片。 |
| 3. 首页与栏目重设计 | 2 至 4 天 | 按新需求重做视觉、信息层级和移动端体验。 |
| 4. 回归检查与发布 | 1 天 | 检查双端布局、外部接口、交互、加载速度和 GitHub Pages。 |

每完成一个阶段，在本文件的历史日程中追加日期、改动和验证结果。
